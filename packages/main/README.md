<h1 align="center">tree-funcs</h1>

Modern JavaScript tree utility library.

## Installation

`tree-funcs` is available as an npm package.

```bash
// with npm
npm install tree-funcs

// with yarn
yarn add tree-funcs
```
